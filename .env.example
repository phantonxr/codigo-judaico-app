VITE_API_BASE_URL=http://localhost:5678/webhook

# IMPORTANT
# - Do NOT put OpenAI keys in Vite/React env vars (they become public in the browser).
# - Keep secrets only on a secure backend (Vercel Functions, Supabase Edge, n8n, etc.).
